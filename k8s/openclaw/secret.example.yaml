apiVersion: v1
kind: Secret
metadata:
  name: openclaw-secrets
  namespace: openclaw
type: Opaque
stringData:
  gateway-token: "CHANGE_ME"  # generate with: openssl rand -hex 32
  gemini-api-key: "CHANGE_ME"
  telegram-bot-token: "CHANGE_ME"
