result
result-*
output/

*.local.*

# K8s secrets (apply manually, don't commit)
k8s/**/secret.yaml
k8s/**/*.secret.yaml

# NixOS secrets (copy to Pi manually, don't commit)
secrets/*
!secrets/*.example
