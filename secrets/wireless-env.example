# WiFi credentials for NixOS
# Copy to wireless-env and fill in real values
# Then copy to Pi: scp secrets/wireless-env ll-raspberry@192.168.0.101:/var/lib/secrets/
#
# Format: key=value (no quotes, values are literal)

WIFI_SSID=your-wifi-name
WIFI_PSK=your-wifi-password
